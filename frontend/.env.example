# Base URL of the FastAPI backend. Must be reachable from the browser.
# Exposed to client code because of the PUBLIC_ prefix (Astro/Vite convention).
# Defaults to http://127.0.0.1:8001 if unset (see src/lib/api.ts).
PUBLIC_API_URL=http://127.0.0.1:8001
